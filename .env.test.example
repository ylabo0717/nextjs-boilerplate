# ==============================================================================
# テスト環境用 環境変数設定（.env.test.example）
# 
# 使用方法:
# 1. このファイルを .env.test にコピー
# 2. 必要に応じて値を調整
# 3. .env.base と組み合わせて使用:
#    テスト実行時に自動的に読み込まれます
#
# 注意: この設定は .env.base の設定を上書きします
# ==============================================================================

# ------------------------------------------------------------------------------
# Environment Configuration - テスト環境設定
# ------------------------------------------------------------------------------

# テスト環境指定
NODE_ENV=test
CI=true

# テスト用ポート設定
PORT=3000
BASE_URL=http://localhost:3000

# ------------------------------------------------------------------------------
# OpenTelemetry Configuration - テスト環境用
# ------------------------------------------------------------------------------

# OpenTelemetry サービス設定（テスト環境用）
OTEL_SERVICE_NAME=nextjs-app-test
OTEL_RESOURCE_ATTRIBUTES=service.name=${OTEL_SERVICE_NAME},service.version=${APP_VERSION}

# テスト環境用OTELエンドポイント
OTEL_EXPORTER_OTLP_ENDPOINT=http://localhost:4318

# ------------------------------------------------------------------------------
# Testing Framework Configuration - テスト専用設定
# ------------------------------------------------------------------------------

# Playwright Configuration
PLAYWRIGHT_SKIP_WEBSERVER=false
PLAYWRIGHT_BASE_URL=http://localhost:3000

# Testcontainers Configuration
# Docker内でTestcontainersを実行するための設定
TESTCONTAINERS_HOST_OVERRIDE=host.docker.internal
DOCKER_HOST=unix:///var/run/docker.sock
TESTCONTAINERS_RYUK_DISABLED=false

# Skip Loki Tests Configuration
# Docker-in-Docker環境でのTestcontainers制約回避のため
SKIP_LOKI_TESTS=true

# ------------------------------------------------------------------------------
# Database Configuration - テスト環境用
# ------------------------------------------------------------------------------

# テスト用データベース
# DATABASE_URL=postgresql://test:test@localhost:5432/test_db

# ------------------------------------------------------------------------------
# Security Configuration - テスト環境用（テスト専用値）
# ------------------------------------------------------------------------------

# テスト用認証設定（本番環境では絶対に使用禁止）
NEXTAUTH_SECRET=test-secret-key-not-for-production
NEXTAUTH_URL=http://localhost:3000

# ------------------------------------------------------------------------------
# Debugging Configuration - テスト環境用
# ------------------------------------------------------------------------------

# デバッグ設定
DEBUG=false

# ------------------------------------------------------------------------------
# IMPORTANT TEST NOTES - テスト環境での注意事項
# ------------------------------------------------------------------------------
#
# 🧪 テスト環境の特徴:
# 1. このファイルはテスト専用です
# 2. 本番環境の秘密情報は絶対に含めないでください
# 3. CIパイプラインでこのファイルが参照される場合があります
# 4. ローカルテスト時は .env.test.local で上書き可能です
#
# ⚠️ セキュリティ注意:
# - テスト用の弱い値・プレースホルダーを使用
# - 本番環境では絶対に使用しないこと
#
# 📝 使用方法:
# - Vitest、Playwright等が自動的にこの設定を読み込みます
# - .env.base の共通設定と組み合わせて使用されます
#
# ==============================================================================