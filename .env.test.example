# ==============================================================================
# Test Environment Variables
# 
# テスト実行時に使用される環境変数設定
# このファイルはテスト専用設定のため、本番環境の秘密情報は含めません
# ==============================================================================

# Node.js Environment
NODE_ENV=test
CI=true
NEXT_TELEMETRY_DISABLED=1

# Application Configuration
PORT=3000
BASE_URL=http://localhost:3000

# Playwright Configuration
PLAYWRIGHT_SKIP_WEBSERVER=false
PLAYWRIGHT_BASE_URL=http://localhost:3000

# Testcontainers Configuration
# Docker内でTestcontainersを実行するための設定
TESTCONTAINERS_HOST_OVERRIDE=host.docker.internal
DOCKER_HOST=unix:///var/run/docker.sock
TESTCONTAINERS_RYUK_DISABLED=false

# Skip Loki Tests Configuration
# Docker-in-Docker環境でのTestcontainers制約回避のため
SKIP_LOKI_TESTS=true

# Test Database (if needed)
# DATABASE_URL=postgresql://test:test@localhost:5432/test_db

# OpenTelemetry Configuration for Testing
OTEL_EXPORTER_OTLP_ENDPOINT=http://localhost:4318
OTEL_SERVICE_NAME=nextjs-app-test
OTEL_RESOURCE_ATTRIBUTES=service.name=nextjs-app-test,service.version=1.0.0

# Debugging (if needed)
DEBUG=false
LOG_LEVEL=info

# Security (test-only values)
NEXTAUTH_SECRET=test-secret-key-not-for-production
NEXTAUTH_URL=http://localhost:3000

# ==============================================================================
# 注意事項
# ==============================================================================
# 
# 1. このファイルはテスト専用です
# 2. 本番環境の秘密情報は絶対に含めないでください
# 3. CIパイプラインでこのファイルが参照される場合があります
# 4. ローカルテスト時は .env.test.local で上書き可能です
# 
# ==============================================================================